<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical"
    android:gravity="center_horizontal">
 
    <!--  Sample Dashboard screen with Two buttons -->
    <!--  Button to view all Restaurants screen -->
    <Button android:id="@+id/btnViewRestaurants"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="@string/ViewRestaurants"
        android:layout_marginTop="25dip"/>
 
    <!--  Button to create a new Restaurants screen -->
    <Button android:id="@+id/btnCreateRViewRestaurants"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="@string/AddRestaurants"
        android:layout_marginTop="25dip"/>
 
</LinearLayout>